export type CartItem = {
  id: number | string; // Unique identifier for the item
  name: string; // Name of the item
  price: number | string; // Price of the item
  quantity: number; // Quantity of the item in the cart
};
